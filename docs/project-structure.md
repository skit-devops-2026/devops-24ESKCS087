# Project structure

This repository contains a static educational website.

- `index.html` is the main entry page.
- `CSS/` stores the stylesheet files.
- `js/` stores browser JavaScript.
- `tests/` contains shell-based validation checks for CI.
- `.github/workflows/ci.yml` defines the automated GitHub Actions checks.
