# Testing

Run the website checks with:

```bash
make test
```

The test script verifies that the main page, stylesheet, and JavaScript entry
files are present and that `index.html` contains the expected HTML markers.
